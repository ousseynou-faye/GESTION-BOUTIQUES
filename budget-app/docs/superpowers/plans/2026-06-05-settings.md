# R6 — Page Settings Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter une page `/settings` avec 3 sections (Profil, Thème, Devise) et propager le choix de devise dans toute l'app via un hook `useFormatMontant`.

**Architecture:** Enrichissement de `settings` avec `nom` et `devise`, 2 nouvelles actions reducer, 1 hook React, 1 page Settings, mise à jour des 5 pages + Sidebar + BottomNav + App.jsx. `formatMontant` reste rétrocompatible (param `devise` optionnel = `'fcfa'`).

**Tech Stack:** React 19, Tailwind CSS v4, BudgetContext (Context API + useReducer), localStorage existant.

---

## Fichiers

| Action | Chemin |
|--------|--------|
| **Modify** | `src/constants/initialState.js` |
| **Modify** | `src/reducers/budgetReducer.js` |
| **Modify** | `src/utils/formatters.js` |
| **Create** | `src/utils/useFormatMontant.js` |
| **Create** | `src/pages/Settings.jsx` |
| **Modify** | `src/App.jsx` |
| **Modify** | `src/components/layout/Sidebar.jsx` |
| **Modify** | `src/components/layout/BottomNav.jsx` |
| **Modify** | `src/pages/Dashboard.jsx` |
| **Modify** | `src/pages/Transactions.jsx` |
| **Modify** | `src/pages/Budgets.jsx` |
| **Modify** | `src/pages/Charts.jsx` |
| **Modify** | `src/pages/Goals.jsx` |

---

### Task 1 : Enrichir le modèle de données et le reducer

**Files:**
- Modify: `src/constants/initialState.js`
- Modify: `src/reducers/budgetReducer.js`

> Pas de test unitaire — le reducer est trivial (spread de settings). La persistence localStorage est couverte implicitement par les tests manuels.

- [ ] **Step 1 : Mettre à jour `src/constants/initialState.js`**

Remplacer le bloc `settings` existant :
```js
settings: {
  theme: 'light',
  devise: 'EUR',
  moisCourant,
},
```
par :
```js
settings: {
  theme: 'light',
  devise: 'fcfa',
  nom: '',
  moisCourant,
},
```

- [ ] **Step 2 : Mettre à jour `src/reducers/budgetReducer.js`**

**2a — Corriger `LOAD_FROM_STORAGE`** pour merger les settings (protection rétrocompatibilité) :

Remplacer :
```js
case 'LOAD_FROM_STORAGE':
  return { ...state, ...action.payload }
```
par :
```js
case 'LOAD_FROM_STORAGE':
  return {
    ...state,
    ...action.payload,
    settings: { ...state.settings, ...action.payload.settings },
  }
```

**2b — Ajouter `SET_NOM` et `SET_DEVISE`** après `SET_MOIS_COURANT` (ligne 102) :

```js
case 'SET_NOM':
  return { ...state, settings: { ...state.settings, nom: action.payload.nom } }
case 'SET_DEVISE':
  return { ...state, settings: { ...state.settings, devise: action.payload.devise } }
```

- [ ] **Step 3 : Vérifier le build**

```bash
npm run build 2>&1 | tail -5
```
Résultat attendu : 0 errors.

- [ ] **Step 4 : Committer**

```bash
git add src/constants/initialState.js src/reducers/budgetReducer.js
git commit -m "feat: add nom/devise to settings, fix LOAD_FROM_STORAGE merge, add SET_NOM/SET_DEVISE"
```

---

### Task 2 : `formatMontant` avec devise + hook `useFormatMontant`

**Files:**
- Modify: `src/utils/formatters.js`
- Create: `src/utils/useFormatMontant.js`

- [ ] **Step 1 : Mettre à jour `formatMontant` dans `src/utils/formatters.js`**

Remplacer la fonction `formatMontant` existante (lignes 4–9) :
```js
export function formatMontant(montant) {
  return new Intl.NumberFormat('fr-FR', {
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(Math.round(montant)) + ' FCFA'
}
```
par :
```js
const SYMBOLES_DEVISE = { fcfa: 'FCFA', eur: 'EUR', usd: 'USD' }

export function formatMontant(montant, devise = 'fcfa') {
  const symbole = SYMBOLES_DEVISE[devise] ?? 'FCFA'
  return new Intl.NumberFormat('fr-FR', {
    minimumFractionDigits: 0,
    maximumFractionDigits: 0,
  }).format(Math.round(montant)) + ' ' + symbole
}
```

- [ ] **Step 2 : Créer `src/utils/useFormatMontant.js`**

```js
import { useBudget } from '@/context/BudgetContext'
import { formatMontant } from '@/utils/formatters'

export function useFormatMontant() {
  const { state } = useBudget()
  const devise = state.settings.devise ?? 'fcfa'
  return (montant) => formatMontant(montant, devise)
}
```

- [ ] **Step 3 : Vérifier le build**

```bash
npm run build 2>&1 | tail -5
```
Résultat attendu : 0 errors.

- [ ] **Step 4 : Lancer les tests**

```bash
npm test -- --run 2>&1 | tail -10
```
Résultat attendu : tous les tests passent (les tests existants n'appellent pas `formatMontant`).

- [ ] **Step 5 : Committer**

```bash
git add src/utils/formatters.js src/utils/useFormatMontant.js
git commit -m "feat: add devise param to formatMontant, create useFormatMontant hook"
```

---

### Task 3 : Page `Settings.jsx`

**Files:**
- Create: `src/pages/Settings.jsx`

- [ ] **Step 1 : Créer `src/pages/Settings.jsx`**

```jsx
import { useState } from 'react'
import { useBudget } from '@/context/BudgetContext'

const DEVISES = [
  { value: 'fcfa', label: 'FCFA' },
  { value: 'eur',  label: 'EUR'  },
  { value: 'usd',  label: 'USD'  },
]

function SettingsCard({ titre, children }) {
  return (
    <div
      className="rounded-2xl overflow-hidden"
      style={{ background: '#0b0e1c', border: '1px solid rgba(255,255,255,0.06)', boxShadow: '0 4px 20px rgba(0,0,0,0.25)' }}
    >
      <div
        className="px-5 py-3.5"
        style={{ borderBottom: '1px solid rgba(255,255,255,0.05)' }}
      >
        <h2 className="font-display text-[13px] font-extrabold tracking-tight" style={{ color: 'rgba(226,232,240,0.92)' }}>
          {titre}
        </h2>
      </div>
      <div className="px-5 py-4">
        {children}
      </div>
    </div>
  )
}

export default function Settings() {
  const { state, dispatch } = useBudget()
  const { nom, theme, devise } = state.settings
  const isDark = theme === 'dark'

  const [nomInput, setNomInput] = useState(nom)

  function handleSaveNom(e) {
    e.preventDefault()
    dispatch({ type: 'SET_NOM', payload: { nom: nomInput.trim() } })
  }

  return (
    <div className="flex flex-col gap-7 animate-fade-slide-up">

      {/* ── Header ── */}
      <div>
        <h1 className="font-display text-2xl font-extrabold leading-tight" style={{ color: 'rgba(226,232,240,0.95)' }}>
          Paramètres
        </h1>
        <p className="text-sm mt-0.5" style={{ color: 'rgba(100,116,139,0.8)' }}>
          Personnalisez votre expérience Budget Pro
        </p>
      </div>

      <div className="flex flex-col gap-4 max-w-lg">

        {/* ── Profil ── */}
        <SettingsCard titre="Profil">
          <form onSubmit={handleSaveNom} className="flex flex-col gap-3">
            <div className="flex flex-col gap-1.5">
              <label className="text-[11px] font-semibold uppercase tracking-[0.12em]" style={{ color: 'rgba(100,116,139,0.7)' }}>
                Prénom
              </label>
              <input
                type="text"
                value={nomInput}
                onChange={e => setNomInput(e.target.value)}
                placeholder="Votre prénom"
                maxLength={32}
                className="input-dark"
              />
            </div>
            <div className="flex justify-end">
              <button type="submit" className="btn-primary text-sm px-4 py-1.5">
                Enregistrer
              </button>
            </div>
          </form>
        </SettingsCard>

        {/* ── Apparence ── */}
        <SettingsCard titre="Apparence">
          <div className="flex gap-2">
            <button
              onClick={() => dispatch({ type: 'SET_THEME', payload: { theme: 'dark' } })}
              className={isDark ? 'btn-primary text-sm px-4 py-1.5' : 'btn-secondary text-sm px-4 py-1.5'}
            >
              Sombre
            </button>
            <button
              onClick={() => dispatch({ type: 'SET_THEME', payload: { theme: 'light' } })}
              className={!isDark ? 'btn-primary text-sm px-4 py-1.5' : 'btn-secondary text-sm px-4 py-1.5'}
            >
              Clair
            </button>
          </div>
        </SettingsCard>

        {/* ── Devise ── */}
        <SettingsCard titre="Devise d'affichage">
          <div className="flex flex-col gap-3">
            <div className="flex gap-2">
              {DEVISES.map(d => (
                <button
                  key={d.value}
                  onClick={() => dispatch({ type: 'SET_DEVISE', payload: { devise: d.value } })}
                  className={devise === d.value ? 'btn-primary text-sm px-4 py-1.5' : 'btn-secondary text-sm px-4 py-1.5'}
                >
                  {d.label}
                </button>
              ))}
            </div>
            <p className="text-[11px]" style={{ color: 'rgba(100,116,139,0.6)' }}>
              Les montants restent stockés en FCFA. Seul le symbole change.
            </p>
          </div>
        </SettingsCard>

      </div>
    </div>
  )
}
```

- [ ] **Step 2 : Vérifier le build**

```bash
npm run build 2>&1 | tail -5
```
Résultat attendu : 0 errors.

- [ ] **Step 3 : Committer**

```bash
git add src/pages/Settings.jsx
git commit -m "feat: add Settings page (Profil, Thème, Devise)"
```

---

### Task 4 : Mettre à jour les pages pour utiliser `useFormatMontant`

**Files:**
- Modify: `src/pages/Dashboard.jsx`
- Modify: `src/pages/Transactions.jsx`
- Modify: `src/pages/Budgets.jsx`
- Modify: `src/pages/Charts.jsx`
- Modify: `src/pages/Goals.jsx`
- Modify: `src/components/layout/Sidebar.jsx`
- Modify: `src/components/goals/QuickDepositCard.jsx`

**Pattern à appliquer dans chaque fichier :**

**Étape A — Import :** remplacer l'import direct de `formatMontant` par le hook.

Si le fichier importe `formatMontant` depuis `formatters` :
```js
// Avant (parmi d'autres imports formatters)
import { formatMontant, ... } from '@/utils/formatters'
```
→ Retirer `formatMontant` de cet import (garder les autres : `formatDate`, `formatMois`, etc.)
→ Ajouter en dessous :
```js
import { useFormatMontant } from '@/utils/useFormatMontant'
```

**Étape B — Hook :** dans le corps du composant principal (function component), ajouter :
```js
const fmt = useFormatMontant()
```

**Étape C — Appels :** remplacer tous `formatMontant(x)` par `fmt(x)` dans ce fichier.

---

**Cas particuliers :**

`src/components/goals/QuickDepositCard.jsx` — importe `{ formatMontant }` depuis `@/utils/formatters` et `{ ProgressBar }`. Même pattern que les autres : retirer `formatMontant` de l'import formatters, ajouter le hook, utiliser `fmt`.

`src/components/layout/Sidebar.jsx` — importe `formatMontant` ligne 5. Même pattern. Le hook `useFormatMontant` utilise `useBudget()` qui est déjà disponible dans Sidebar.

`src/components/pdf/RapportMensuel.jsx` — **NE PAS MODIFIER** — le PDF garde `formatMontant` direct (toujours FCFA).

- [ ] **Step 1 : Mettre à jour les 7 fichiers** selon le pattern ci-dessus

Pour chaque fichier, l'ordre est :
1. Retirer `formatMontant` de l'import existant
2. Ajouter `import { useFormatMontant } from '@/utils/useFormatMontant'`
3. Ajouter `const fmt = useFormatMontant()` en haut du composant
4. Remplacer `formatMontant(` par `fmt(` dans tout le fichier

- [ ] **Step 2 : Vérifier le build**

```bash
npm run build 2>&1 | tail -10
```
Résultat attendu : 0 errors.

- [ ] **Step 3 : Lancer les tests**

```bash
npm test -- --run 2>&1 | tail -10
```
Résultat attendu : 49 tests passent.

- [ ] **Step 4 : Committer**

```bash
git add src/pages/Dashboard.jsx src/pages/Transactions.jsx src/pages/Budgets.jsx src/pages/Charts.jsx src/pages/Goals.jsx src/components/layout/Sidebar.jsx src/components/goals/QuickDepositCard.jsx
git commit -m "feat: propagate devise setting via useFormatMontant in all pages"
```

---

### Task 5 : Navigation — App.jsx + Sidebar + BottomNav

**Files:**
- Modify: `src/App.jsx`
- Modify: `src/components/layout/Sidebar.jsx`
- Modify: `src/components/layout/BottomNav.jsx`

- [ ] **Step 1 : Ajouter la route `/settings` dans `src/App.jsx`**

Ajouter l'import de la page Settings après les imports existants (ligne 10) :
```js
import Settings from '@/pages/Settings'
```

Dans le bloc `<Routes>`, ajouter après la route `/objectifs` :
```jsx
<Route path="/settings" element={<Settings />} />
```

- [ ] **Step 2 : Mettre à jour `src/components/layout/Sidebar.jsx`**

**2a — Ajouter l'item "Paramètres" dans `navItems`** (après l'item "Objectifs", avant la fermeture du tableau ligne 66) :

```js
{
  to: '/settings',
  label: 'Paramètres',
  icon: (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
      strokeWidth={1.8} strokeLinecap="round" strokeLinejoin="round" className="w-[17px] h-[17px]" aria-hidden="true">
      <path d="M12 15a3 3 0 100-6 3 3 0 000 6z" />
      <path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z" />
    </svg>
  ),
},
```

**2b — Afficher "Bonjour, {nom}" sous le logo** dans la section Brand :

Trouver le bloc `<div className="min-w-0">` dans la Sidebar (section Brand). À l'intérieur, après le `<p>` qui affiche "Gestion financière", ajouter :
```jsx
{state.settings.nom && (
  <p className="text-[10px] mt-1 truncate" style={{ color: 'rgba(129,140,248,0.55)' }}>
    Bonjour, {state.settings.nom}
  </p>
)}
```

- [ ] **Step 3 : Mettre à jour `src/components/layout/BottomNav.jsx`**

Ajouter l'item "Réglages" dans `navItems` (après l'item "Objectifs", avant la fermeture du tableau ligne 62) :

```js
{
  to: '/settings',
  label: 'Réglages',
  icon: (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
      strokeWidth={1.75} strokeLinecap="round" strokeLinejoin="round" className="w-[22px] h-[22px]" aria-hidden="true">
      <path d="M12 15a3 3 0 100-6 3 3 0 000 6z" />
      <path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z" />
    </svg>
  ),
},
```

- [ ] **Step 4 : Vérifier le build**

```bash
npm run build 2>&1 | tail -5
```
Résultat attendu : 0 errors.

- [ ] **Step 5 : Test manuel**

Lancer `npm run dev` et vérifier :
1. La Sidebar affiche "Paramètres" dans la navigation
2. Le BottomNav affiche "Réglages" (6ème item)
3. Cliquer "Paramètres" → la page `/settings` s'affiche
4. Saisir un prénom → "Enregistrer" → la Sidebar affiche "Bonjour, {prénom}"
5. Basculer Thème → l'app change de thème
6. Choisir "EUR" → tous les montants affichent "EUR"
7. Recharger la page → les 3 réglages persistent

- [ ] **Step 6 : Committer**

```bash
git add src/App.jsx src/components/layout/Sidebar.jsx src/components/layout/BottomNav.jsx
git commit -m "feat: add /settings route, Settings nav items, Bonjour nom in Sidebar"
```
